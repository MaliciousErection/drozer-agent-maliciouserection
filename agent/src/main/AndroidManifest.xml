<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mwr.dz"
    android:versionCode="8"
    android:versionName="2.5.2" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.AboutActivity"
            android:label="@string/title_about"
            android:parentActivityName="com.mwr.dz.activities.SettingsActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/title_activity_main"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.StartBroadcastActivity"
            android:exported="true" />
        <activity android:name=".helpers.IntentProxyToContentProvider"
            android:exported="true" />
        <activity
            android:name=".activities.EndpointActivity"
            android:label="@string/title_activity_endpoint" />
        <activity
            android:name=".activities.EndpointSettingsActivity"
            android:label="@string/endpoint_new" />
        <activity
            android:name=".activities.ServerActivity"
            android:label="@string/title_activity_server" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/title_activity_settings" />
        
        <receiver 
            android:name=".receivers.Receiver_StartService"
            android:exported="true"
            android:process=":remote">
        </receiver>
        <receiver android:name=".receivers.Receiver_StartActivity"
            android:exported="true"
            android:process=":remote">
        </receiver>

        <service
            android:name=".services.ClientService"
            android:process=":remote" />
        <service
            android:name=".services.ServerService"
            android:process=":remote" />
        <service
            android:name="com.mwr.dz.services.SessionService"
            android:process=":remote" />

        <provider
            android:authorities=".providers.Provider"
            android:name=".providers.Provider" />
    </application>

</manifest>
